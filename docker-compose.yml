services:
  postgres:
    image: postgres:16-alpine
    container_name: cards-postgres
    environment:
      POSTGRES_DB: cards
      POSTGRES_USER: cards
      POSTGRES_PASSWORD: cards
    ports:
      - "5432:5432"
    volumes:
      - cards-postgres-data:/var/lib/postgresql/data
volumes:
  cards-postgres-data:
